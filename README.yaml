name: "aws-saml"
# Canonical GitHub repo
github_repo: "cloudposse-terraform-components/aws-saml"
# Short description of this project
description: |-
  This component provisions SAML metadata into AWS IAM as new SAML providers. For Okta integrations (when `okta` is
  included in the key provided to the `saml_providers` input), it also creates an Okta API user and an associated Access
  Key pair, and stores the credentials in AWS SSM Parameter Store.
usage: |-
  Stack Level: Global, in the account to which users will log in, typically only `identity`.

  Here's an example snippet for how to use this component.

  IMPORTANT: The given SAML metadata files must exist at the root of the module.

  ```yaml
  components:
    terraform:
      aws-saml:
        vars:
          enabled: true
          saml_providers:
            enabled: true
            example-okta: Okta_metadata_example.xml
            example-gsuite: GoogleIDPMetadata-example.com.xml
  ```

  <!-- prettier-ignore-start -->
  <!-- prettier-ignore-end -->
references:
  - name: cloudposse-terraform-components
    url: https://github.com/orgs/cloudposse-terraform-components/repositories
    description: "Cloud Posse's upstream component"
tags:
  - component/aws-saml
  - layer/identity
  - provider/aws
  - privileged
# Categories of this project
categories:
  - component/aws-saml
  - layer/identity
  - provider/aws
  - privileged
# License of this project
license: "APACHE2"
# Badges to display
badges:
  - name: Latest Release
    image: https://img.shields.io/github/release/cloudposse-terraform-components/aws-saml.svg?style=for-the-badge
    url: https://github.com/cloudposse-terraform-components/aws-saml/releases/latest
  - name: Slack Community
    image: https://slack.cloudposse.com/for-the-badge.svg
    url: https://slack.cloudposse.com
related:
  - name: "Cloud Posse Terraform Modules"
    description: Our collection of reusable Terraform modules used by our reference architectures.
    url: "https://docs.cloudposse.com/modules/"
  - name: "Atmos"
    description: "Atmos is like docker-compose but for your infrastructure"
    url: "https://atmos.tools"
contributors: [] # If included generates contribs
